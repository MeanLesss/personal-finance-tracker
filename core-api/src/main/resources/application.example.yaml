spring:
  profiles:
    active: local
  application:
    name: personal-finance-tracker

  datasource:
    url: "jdbc:postgresql://localhost:5432/personal_finance_db"
    username: postgres
    password: 
  jpa:
    hibernate:
      ddl-auto: "update"
    show-sql: true
    properties:
      hibernate:
        format-sql: true
server:
  port: 9090