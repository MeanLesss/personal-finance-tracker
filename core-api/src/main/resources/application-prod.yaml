spring:
  profiles:
    active: prod
  application:
    name: personal-finance-tracker

  datasource:
    url: "jdbc:postgresql://localhost:5433/personal_finance_db"
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: "update"
    show-sql: true
    properties:
      hibernate:
        format-sql: true
server:
  port: 9090